<%= form_for(@gksection) do |f| %>
  <% if @gksection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gksection.errors.count, "error") %> prohibited this gksection from being saved:</h2>

      <ul>
      <% @gksection.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
